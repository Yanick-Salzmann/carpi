#ifndef CARPI_SAVED_ITEMS_HPP
#define CARPI_SAVED_ITEMS_HPP

#include <string>
#include "show_simplified.hpp"
#include "track_simplified.hpp"
#include "album_simplified.hpp"

namespace carpi::spotify::web::model {
    /*
    struct saved_track {
        std::string added_at;
        track track;
    };

    struct saved_album {
        std::string added_at;
        album album;
    };

    struct saved_show {
        std::string added_at;
        show show;
    };
     */
}

#endif //CARPI_SAVED_ITEMS_HPP
